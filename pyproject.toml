[project]
name = "k8s-data-platform-spark-apps"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "minio>=7.2.15",
    "pyiceberg>=0.9.1",
    "pyspark==3.5.5",
]
